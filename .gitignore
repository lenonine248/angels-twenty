# OS が作る余計なファイル
Thumbs.db
desktop.ini
.DS_Store

# エディタ・ツールのローカル設定
.vscode/
.idea/
*.swp
.claude/

# Python
__pycache__/
*.pyc

# ローカルのプレイ記録（難易度調整用・環境依存なので共有しない）
playlog.jsonl

# 戦闘のリプレイ（§23）。**手元だけに置く。**
# 1本 40〜800KB あり、版を切るたびに録り直すので、共有すると膨らみ続ける。
# 公開版では一覧の受け口（開発サーバの `/replays/index.json`）が無いので、
# 置いてあっても誰も開けない —— ファイル選択に落ちるだけで動きは変わらない。
replays/
